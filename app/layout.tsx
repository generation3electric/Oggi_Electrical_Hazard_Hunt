import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Oggi’s Electrical Hazard Hunt | GEN3 Electric & HVAC",
  description:
    "Explore five Philadelphia homes with Oggi and find the hidden electrical hazards before time runs out.",
  openGraph: {
    title: "Can You Sniff Out the Hazards?",
    description:
      "Play Oggi’s Electrical Hazard Hunt from GEN3 Electric & HVAC.",
    type: "website",
  },
  icons: {
    icon: "/favicon.svg",
    shortcut: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
